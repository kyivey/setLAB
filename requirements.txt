# Python packages for SetLAB (install with: pip install -r requirements.txt)
# System tools (ffmpeg, deno, Python itself) come from Homebrew -- see Brewfile.

# GUI
PyQt5==5.15.11   # pip picks the matching PyQt5-Qt5 / PyQt5-sip for your Mac

# Only needed to build SetLAB.app
pyinstaller==6.11.1
